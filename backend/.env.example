
#  run with docker compose --env-file ./backend/.env up -d — not plain docker compose up -d
PORT=8080
APP_ENV=development

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=habeshan_rems
DB_PORT=5432

JWT_SECRET=change_me_to_a_32_plus_char_random_string
JWT_EXPIRES_IN=24h

FRONTEND_URL=http://localhost:5173

TELEGRAM_BOT_TOKEN=

RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=20

